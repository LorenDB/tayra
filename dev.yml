version: "3"

# Local development compose. Vue front removed — run Tayra separately:
#   flutter run -d chrome --dart-define=FUNKWHALE_URL=http://localhost:5000
# Or build SPA into front/dist and use the production front image.

services:
  # Local dev still defaults to trust/no-auth for convenience. Production
  # docker-compose.yml requires passwords (M5). Prefer passwords here too when
  # exposing ports beyond the internal network.
  postgres:
    env_file:
      - .env.dev
      - .env
    image: postgres:${POSTGRES_VERSION-11}-alpine
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=${POSTGRES_HOST_AUTH_METHOD:-trust}"
    command: postgres ${POSTGRES_ARGS-}
    volumes:
      - "./data/${COMPOSE_PROJECT_NAME-node1}/postgres:/var/lib/postgresql/data"
    networks:
      - internal

  redis:
    env_file:
      - .env.dev
      - .env
    image: redis:7-alpine
    volumes:
      - "./data/${COMPOSE_PROJECT_NAME-node1}/redis:/data"
    networks:
      - internal

  api:
    env_file:
      - .env.dev
      - .env
    build:
      context: ./api
      target: dev
    volumes:
      - "./api:/app"
    networks:
      - internal
    depends_on:
      - postgres
      - redis

  celeryworker:
    env_file:
      - .env.dev
      - .env
    build:
      context: ./api
      target: dev
    command:
      - celery
      - --app=funkwhale_api.taskapp
      - worker
      - --loglevel=INFO
    volumes:
      - "./api:/app"
    networks:
      - internal
    depends_on:
      - postgres
      - redis

  celerybeat:
    env_file:
      - .env.dev
      - .env
    build:
      context: ./api
      target: dev
    command:
      - celery
      - --app=funkwhale_api.taskapp
      - beat
      - --loglevel=INFO
    volumes:
      - "./api:/app"
    networks:
      - internal
    depends_on:
      - postgres
      - redis

  # Optional: serve pre-built Tayra from front/dist
  front:
    profiles: ["front"]
    build:
      context: front
      dockerfile: Dockerfile
      target: production
    env_file:
      - .env.dev
      - .env
    ports:
      - "${VUE_PORT-8080}:80"
    networks:
      - internal
    depends_on:
      - api

networks:
  internal:
